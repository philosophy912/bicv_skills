# 缺陷文档模板

> 复制本文件为 `issue-<日期>-<简述>.md`（如 `issue-2026-07-02-card-500.md`），按下列小节填写。
> 纯新增功能、文档、重构类改动**不需要**写 issue 文档——只有修 bug 才写。

---

# <一句话标题>

- **日期**：YYYY-MM-DD
- **影响 skill**：<skill-name> / 全局
- **严重程度**：阻断 / 影响功能 / 体验问题

## 问题描述

- 现象：<观察到的错误行为>
- 复现步骤：
  1. ...
  2. ...
- 影响：<哪些场景/用户受影响>

## 根本原因

<定位过程 + 真正的根因。不是表象（"报错 X"），而是为什么报错（如"某分支漏处理 None，导致……"）。指到具体代码位置 `file:line` 更好。>

## 解决方案

- 改动：<具体改了什么>
- 为什么这么改：<设计取舍 / 为何选这个方案>
- 关联文件：
  - `path/to/file.py`
  - `...`

## 验证

- 新增测试：<测试用例名 / 覆盖的分支>
- 手动验证：<操作 + 预期结果 + 实际结果>
- 回归确认：<确认没破坏其它功能>

## 后续（可选）

- 是否需要同步更新 `docs/spec/` 或 `README.md`：
- 是否有相关遗留问题：
